{
  "name": "reator-web",
  "version": "4.2.2",
  "description": "A web-based editor with realtime collaboration and voice calls.",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "standard --fix src/** && standard --fix test/** && tape test/*.js",
    "build": "browserify src/index.js -i wrtc -s Reator -o static/js/reator.js",
    "watch": "watchify src/index.js -i wrtc -s Reator -o static/js/reator.js"
  },
  "author": "Reator",
  "license": "MIT",
  "devDependencies": {
    "standard": "^12.0.1",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "clipboard": "^2.0.1",
    "cuid": "^2.1.4",
    "events": "^3.0.0",
    "getusermedia": "^2.0.1",
    "inherits": "^2.0.3",
    "multihack-core": "^2.1.7",
    "mustache": "^3.0.0",
    "p2p-graph": "^1.1.1"
  }
}
